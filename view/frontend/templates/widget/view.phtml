<?php
 $slides = $block->getSlides();
 if(count($slides)):
?>

 <div id="<?= $block->getId() ?>" class="usslick__slider">

  <?php
   foreach($slides as $slide):
    $status = $slide['status'];
    $title = $slide['title'];
    $alt = $slide['alt'];
    $link = $slide['link'];
    $image = $slide['image'];
    $getLink = $block->getSlideLink($link);
    $getImage = $block->getSlideImage($image);
    $isNewTab = $block->getSlideTarget($link);
   ?>
   
  <?php if($status && $image): ?>
  <div class="usslick__slide">
    
   <?php if($getLink) : ?>
   <a href="<?= $getLink ?>" title="<?= $title ?>" class="usslick__slide--link" <?= ($isNewTab) ? 'target="_blank"': '' ?>>
   <?php else: ?>
   <div class="usslick__slide--unlink">
   <?php endif; ?>

    <img src="<?= $getImage ?>" alt="<?= $alt ?>" title="<?= $title ?>" class="usslick__slide--image" />

   <?php if($getLink) : ?>
   </a>
   <?php else: ?>
   </div>
   <?php endif; ?>

  </div>
  <?php endif; ?>

  <?php endforeach; ?>

 </div>

<?php endif; ?>

<pre><?= json_encode($this->getData('slider_data'),JSON_PRETTY_PRINT) ?></pre>

<script>
 require([
  'jquery',
  'matchMedia',
  'slick'
 ], function($, mediaCheck, slick) {

   var $slick_slider = $('#hellloooo03');
   $slick_slider.slick({ "mobileFirst": true, "dots": true, "infinite": false, "speed": 300, "slidesToShow": 1, "slidesToScroll": 1 });


   mediaCheck({
    media: '(max-width: 699px)',
    entry: function() {
     
     if($slick_slider.hasClass('slick-initialized'))
      $slick_slider.slick('unslick');
     return
    },
    exit: function() {
     if(!$slick_slider.hasClass('slick-initialized'))
      return $slick_slider.slick("resize");;
     }
   });



 });
</script>

<div id="hellloooo03" class="usslick__slider" data-slick='{ "responsive": [ { "breakpoint": 1200, "settings": "unslick" }, { "breakpoint": 1024, "settings": { "slidesToShow": 3, "slidesToScroll": 3, "infinite": true, "dots": true } }, { "breakpoint": 700, "settings": { "slidesToShow": 2, "slidesToScroll": 2 } } ] }'>
      <div><h3>1</h3></div>
      <div><h3>2</h3></div>
      <div><h3>3</h3></div>
      <div><h3>4</h3></div>
      <div><h3>5</h3></div>
      <div><h3>6</h3></div>
    </div>

<pre><?= $block->getStylesheets(); ?></pre>




